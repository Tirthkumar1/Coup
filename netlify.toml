[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # These are public-facing keys used by the Vite client-side bundle.
  # Netlify's secret scanner should ignore them to allow the build to complete.
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_ANON_KEY,VITE_SUPABASE_URL"
